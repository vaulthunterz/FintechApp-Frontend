import React from 'react';
import { ToastConfig } from 'react-native-toast-message';

// A minimal toast configuration that works with react-native-toast-message
export const toastConfig: ToastConfig = {
  success: () => null,
  error: () => null,
  info: () => null
};

